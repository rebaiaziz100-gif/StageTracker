package com.devnet.stagetracker.repositories;

import com.devnet.stagetracker.entities.StagePfe;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StagePfeRepository extends JpaRepository<StagePfe, Integer> {
}