package com.devnet.stagetracker.repositories;

import com.devnet.stagetracker.entities.StageEte;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StageEteRepository extends JpaRepository<StageEte, Integer> {
}